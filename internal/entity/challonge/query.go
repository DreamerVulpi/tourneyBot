package challonge

const (
	GetTournament = "tournaments/%s.json"
	GetMatches    = "tournaments/%s/matches.json"
	// TODO: Need to check work ability
	GetCheckIn     = "tournaments/%s/participants/%s/check_in.json"
	GetParticipant = "tournaments/%s/participants/%s.json"
)
